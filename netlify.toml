[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

# Official Next.js runtime for Netlify (App Router, Server Actions, middleware).
[[plugins]]
  package = "@netlify/plugin-nextjs"
